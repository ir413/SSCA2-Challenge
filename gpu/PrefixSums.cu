#include "PrefixSums.h"


__global__ void prefixSumsKernel(int *data, int *sums, size_t size)
{
  (void*) data;
  (void*) sums;
  (void*) size;
}

void prefixSums(int *data, int *sums, size_t size)
{
  (void*) data;
  (void*) sums;
  (void*) size;
}
